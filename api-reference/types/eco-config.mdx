---
title: 'EcoConfig'
description: 'Configuration type for the EcoProvider'
---

## Overview

The `EcoConfig` type defines the configuration object that is passed to the `EcoProvider`. It contains the necessary settings for the Eco React SDK to function properly.

## Type Definition

```tsx
interface EcoConfig {
  apiKey: string;
  baseUrl?: string;
}
```
