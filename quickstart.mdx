---
title: "Quickstart"
description: "Get started with the Eco React SDK in under 5 minutes"
---

## Installation

Install the Eco React SDK and its peer dependencies:

<Tabs>
  <Tab title="npm">
    ```bash
    npm install @eco-foundation/react-sdk
    npm install @tanstack/react-query wagmi @wagmi/core viem @eco-foundation/routes-sdk @eco-foundation/routes-ts
    ```
  </Tab>
  <Tab title="yarn">
    ```bash
    yarn add @eco-foundation/react-sdk
    yarn add @tanstack/react-query wagmi @wagmi/core viem @eco-foundation/routes-sdk @eco-foundation/routes-ts
    ```
  </Tab>
  <Tab title="pnpm">
    ```bash
    pnpm add @eco-foundation/react-sdk
    pnpm add @tanstack/react-query wagmi @wagmi/core viem @eco-foundation/routes-sdk @eco-foundation/routes-ts
    ```
  </Tab>
</Tabs>

## Setup

### 1. Configure Wagmi

Set up your wagmi configuration with the chains you want to support:

<CodeGroup>

```tsx App.tsx
import { EcoProvider } from "@eco-foundation/react-sdk";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { WagmiProvider } from "wagmi";
import { config } from "./wagmi-config";

const queryClient = new QueryClient();

function App() {
  return (
    <WagmiProvider config={config}>
      <QueryClientProvider client={queryClient}>
        <EcoProvider
          config={{
            apiKey: "your-api-key",
            baseUrl: "https://api.eco.org",
          }}
        >
          <YourApp />
        </EcoProvider>
      </QueryClientProvider>
    </WagmiProvider>
  );
}
```

```tsx wagmi-config.ts
import { createConfig, http } from "wagmi";
import {
  mainnet,
  polygon,
  optimism,
  base,
  arbitrum,
  bsc,
  avalanche,
} from "wagmi/chains";
import { injected, walletConnect } from "wagmi/connectors";

export const config = createConfig({
  chains: [mainnet, polygon, optimism, base, arbitrum, bsc, avalanche],
  connectors: [
    injected(),
    walletConnect({ projectId: "your-walletconnect-project-id" }),
  ],
  transports: {
    [mainnet.id]: http(),
    [polygon.id]: http(),
    [optimism.id]: http(),
    [base.id]: http(),
    [arbitrum.id]: http(),
    [bsc.id]: http(),
    [avalanche.id]: http(),
  },
});
```

</CodeGroup>

## Your First Intent

Create and publish your first cross-chain intent:

```tsx
import { usePublishIntent } from '@eco-foundation/react-sdk';
import { useAccount } from 'wagmi';

function IntentPublisher() {
  const { address } = useAccount();
  const { publishIntent, isLoading, error } = usePublishIntent({
    onSuccess: (result) => {
      console.log('Intent published successfully!');
      console.log('Publish transaction:', result.publishTxHash);
      console.log('Approval transactions:', result.approvalTxHashes);
    },
    onError: (error) => {
      console.error('Failed to publish intent:', error);
    }
  });

  const handlePublishIntent = () => {
    if (!address) {
      alert('Please connect your wallet first');
      return;
    }

    publishIntent({
      creator: address,
      originChainID: 1, // Ethereum
      destinationChainID: 137, // Polygon
      receivingToken: '0xA0b86a33E6441b8c4C8C0C8C0C8C0C8C0C8C0C8C', // USDC on Polygon
      spendingToken: '0xB0b86a33E6441b8c4C8C0C8C0C8C0C8C0C8C0C8C', // USDC on Ethereum
      spendingTokenLimit: BigInt('1000000000'), // 1000 USDC (6 decimals)
      amount: BigInt('500000000'), // 500 USDC (6 decimals)
      recipient: address,
      prover: 'HyperProver',
    });
  };

  return (
    <div>
      <button
        onClick={handlePublishIntent}
        disabled={isLoading || !address}
        className="px-4 py-2 bg-green-500 text-white rounded disabled:opacity-50"
      >
        {isLoading ? 'Publishing...' : 'Publish Intent'}
      </button>
      {error && <p className="text-red-500 mt-2">{error.message}</p>}
    </div>
  );
}
```

## Next Steps

<CardGroup cols={2}>
  <Card title="Hooks" icon="code" href="/api-reference/hooks/use-publish-intent">
    Browse the collection of React Hooks and learn how to use them
  </Card>
  <Card title="Installation" icon="download" href="/essentials/installation">
    Install the SDK and its dependencies
  </Card>
  <Card title="Setup" icon="wrench" href="/essentials/setup">
    Configure the SDK in your application
  </Card>
  <Card title="Open Quoting Client" icon="code" href="/api-reference/hooks/use-open-quoting-client">
    Learn how to request and manage quotes
  </Card>
</CardGroup>
